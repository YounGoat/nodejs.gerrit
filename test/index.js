'use strict';

const MODULE_REQUIRE = 1
    /* built-in */
    , assert = require('assert')

    /* NPM */
    
    /* in-package */
    ;

describe('EMPTY', () => {
    it('SEE /example/ for test units', () => {});
});